A view that concatenates the ingredients into a recipe would be nice.

Script/view to list cocktails that would be enabled by missing ingredients

Add columns:
    Cocktails:
        Own creation (bool)
        Link

Added Raspberry liqueur twice - remove one, and add check when adding ingredients

Flask:
    Running in VS code: https://code.visualstudio.com/docs/python/tutorial-flask
        Command Palette -> Create Environment (Ctrl+Alt+E)
        Wait for it to finish
        Create new terminal (Ctrl+Shift+`)
        Connect to VPN for pip
        python -m pip install flask (on VPN)
        pip install psycopg2
        python -m flask run
